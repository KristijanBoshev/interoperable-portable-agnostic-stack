apiVersion: v1
kind: Service
metadata:
  name: todo-api
  labels:
    app.kubernetes.io/name: todo-api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: todo-api
  ports:
    - name: http
      port: 80
      targetPort: http
